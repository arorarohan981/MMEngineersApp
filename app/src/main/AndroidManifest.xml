<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rohan.mmengineerco">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MMEngineerCo">
        <activity android:name=".ProductDetailsActivity"></activity>
        <activity android:name=".Category1Activity" />
        <activity android:name=".Admin_Modify_Product" />
        <activity android:name=".admin_products_Category" />
        <activity android:name=".Admin_Add_New_Product" />
        <activity android:name=".admin_product_activity" />
        <activity
            android:name=".Navigation_Drawer_homescreen"
            android:label="@string/title_activity_navigation__drawer_homescreen"
            android:theme="@style/Theme.MMEngineerCo.NoActionBar" />
        <activity android:name=".CategoryScreen" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>